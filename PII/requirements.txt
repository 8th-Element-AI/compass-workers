# ── Core (always required) ───────────────────────────────────────────────────
spacy==3.8.14
Faker==40.21.0
PyYAML==6.0.3
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# ── HuggingFace NER models (required when ner_model is a HF model ID) ────────
# ner_model="gravitee-io/bert-small-pii-detection"  (default)
# ner_model="dslim/bert-base-NER"
# ner_model="dslim/distilbert-NER"
# ner_model="iiiorg/piiranha-v1-detect-personal-information"
# ner_model="obi/deid_roberta_i2b2"
torch==2.12.0
transformers==4.57.6
spacy-huggingface-pipelines==0.0.4
huggingface-hub>=0.20.0

# ── REST API (deidentifier/api.py) ──────────────────────────────────────────
fastapi>=0.110.0
pydantic==2.13.4
uvicorn>=0.29.0

# ── Dev / testing ────────────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0

# ── Evaluation (eval/evaluate.py) ────────────────────────────────────────────
kaggle>=1.6

# ── Post-install: download spaCy models ─────────────────────────────────────
# en_core_web_sm is always required (tokenizer for all modes)
#   python -m spacy download en_core_web_sm
#
# en_core_web_lg is required when ner_model="en_core_web_lg"
#   python -m spacy download en_core_web_lg
#
# Pre-cache a HuggingFace model before first use (no downloads at runtime):
#   python -c "from transformers import pipeline; pipeline('ner', model='gravitee-io/bert-small-pii-detection', aggregation_strategy='first')"
#
# Download the eval dataset from Kaggle (kaggle CLI installed via the dependency above):
#   pip install kaggle
#   kaggle datasets download alejopaullier/pii-external-dataset -p eval/data --unzip